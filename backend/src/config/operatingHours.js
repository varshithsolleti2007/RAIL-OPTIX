// The section's daily maintenance window. Real, already-governing
// constraint (used by candidate-window generation) - reused here rather
// than invented, so the day simulation checks against the same bound
// requests are actually scheduled within.
export const DAY_START_HOUR = 6;
export const DAY_END_HOUR = 22;
